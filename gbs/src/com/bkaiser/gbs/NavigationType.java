package com.bkaiser.gbs;

public enum NavigationType {
	TABS,
	PILLS,
	PILLSTACK,
	NAVBAR
}

// button (navbar-btn) are directly handled in GbsButton with setNavbar()
// dito with TEXT and FORM (but not supported yet)